1.逻辑
逻辑线程按每一个链接进行散列
保证每个链接的处理是单线程的，避免并发。

1.数据
本地缓存和远端缓存是即时更新的。

数据库为异步数据库操作，按类型和该类型值进行散列
每次指令逻辑产生的数据库操作保存到对应GroupKey的IDaoOperateStore中，在指令完成的时候统一创建任务丢到对应的线程中去。

-----------
注意：
异步数据库操作，必须用本地缓存，否则拿不到最新的数据。